const authRoutes = require("./auth-routes");
const userRoutes = require("./user-routes");
const listingRoutes = require("./listing-routes");

module.exports = { authRoutes, userRoutes, listingRoutes };
