const authControllers = require("./auth-controllers");
const userControllers = require("./user-controllers");
const listingControllers = require("./listing-controllers");
const chatControllers = require("./chat-controllers");
const locationControllers = require("./location-controllers");

module.exports = {
  authControllers,
  userControllers,
  listingControllers,
  chatControllers,
  locationControllers,
};
